# FE0 卡池筛选与自动构筑工具说明

## 文件位置
- 工具脚本: deckBuilder.js
- 命令入口: package.json 中的 deck:analyze 与 deck:build

## 目标
该工具用于把基于 FE0 规则读取卡池并筛选卡片的流程标准化，便于后续重复调用，而不是每次手动分析。

## 能力概览
- 读取卡池数据并完成基础清洗
- 按势力进行卡池分析与候选排序
- 按启发式评分筛选单卡
- 按角色线与主轴生成 50 张推荐卡组
- 执行合法性校验
- 可选: 保存到公开卡组库或隐藏卡组库

## 关键规则口径
- 目标卡组规模: 50 张
- 单卡上限: 4 张
- 默认优先单势力，提高费用与协同稳定性
- 优先保留低费展开、稳定支援、关键终结与资源能力

## 常用命令
- 分析卡池:
  - npm run deck:analyze
- 自动构筑:
  - npm run deck:build
- 指定势力构筑:
  - node deckBuilder.js --build --force 圣痕
- 构筑并保存为隐藏卡组:
  - node deckBuilder.js --build --save --password AI --name AI 自动推荐
- 输出 JSON:
  - node deckBuilder.js --build --json

## 参数说明
- --analyze: 分析模式
- --build: 构筑模式
- --force 势力名: 指定势力
- --pool 路径: 指定卡池文件
- --save: 保存推荐卡组
- --password 口令: 保存到隐藏卡组时使用
- --name 名称: 指定保存名称
- --json: JSON 输出

## 当前实现边界
- 这是启发式策略工具，不是完整对局搜索器
- 评分与主轴选择面向可用性与稳定性，非数学最优解
- 若后续能力引擎新增复杂文本语义，建议同步更新评分特征

## 可选代办事件
以下代办默认不自动执行，只有在你明确确认后才实施。

### TODO-OPT-01
新增 --archetype 参数，支持以下构筑风格:
- 中速
- 飞兵速攻
- 手牌破坏

目标收益:
- 同势力下可快速产出多风格卡组
- 便于对局前按环境切换方案

### TODO-OPT-02
将 deckBuilder.js 的核心逻辑拆分为可导入模块，例如 modules/deckBuilderCore.js。

目标收益:
- 前端、服务端、脚本可复用同一套构筑核心
- 降低后续维护成本

## 未来触发约定
当你让我读取本文件时，我会先询问你:
- 是否现在真正实现 TODO-OPT-01
- 是否现在真正实现 TODO-OPT-02

只有在你确认后，我才会进入对应代办的实际开发流程。
